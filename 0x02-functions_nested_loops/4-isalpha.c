#include "main.h"
/**
 *_isalpha - checks for characters in uppercase
 *@c: character parameter
 *Return: returns 1 or 0, depending on the condition
 */

int _isalpha(int c)
{
	return (c >= 'A' && c <= 'Z');
}
