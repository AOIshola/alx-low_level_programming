#include <stdio.h>
/**
 *main - Entry point
 *Description: 'print single-digit numbers in base 10'
 *Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	n = 0;
	while
		(n < 10)
		{
			printf("%d\n", n);
			n++;
		}
	return (0);
}
